// Edit `pieces` below to customize the multi-sprite avatar.
// Pieces must have an x,y offset and a sprite id.

// Provided example is a 2x2 square for simplicity,
// but multi-sprite avatar's shape can be arbitrary.

// These are sample values, and these must be edited for this hack to work.

pieces: [{
	x: 0,
	y: 0,
	spr: 'c'
}, {
	x: 1,
	y: 0,
	spr: 'd'
}, {
	x: 0,
	y: 1,
	spr: 'e'
}, {
	x: 1,
	y: 1,
	spr: 'f'
}],
enabledOnStart: true