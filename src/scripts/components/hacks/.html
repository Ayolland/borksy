<p>These are pre-loaded hacks that modify and extend Bitsy's behavior. Just check the box to include the hack. Hacks with options
	have a expandable section with a modifiable JavaScript options object. If you need a hack not seen here, use the Additional
	JavaScript field instead.</p>
